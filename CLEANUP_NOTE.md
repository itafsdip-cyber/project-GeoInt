# Repository Cleanup Note

This commit ensures the `manual-pr-fix` branch is intentionally ahead of `main` so a fresh pull request can be created without stale PR linkage issues.

- Added a branch-stabilization note.
- Verified ignore rules for common Node/Vite artifacts in the app scope.
